# Copy this file to .env and fill in real values
# cp .env.example .env

# Database password (MUST change in production)
POSTGRES_PASSWORD=yingo123

# Security secrets (MUST change in production)
PEPPER_SECRET=dev-pepper-change-in-production
TOKEN_SECRET=dev-token-secret-change-in-production
INTERNAL_API_KEY=dev-internal-key-change-in-production

# Admin username (optional; the first registered user automatically becomes admin)
ADMIN_USERNAME=

# SSL (optional; leave empty to use HTTP)
SSL_CERT=
SSL_KEY=
